class Solution:
    def toLowerCase(self, str: str) -> str:
        ans = ''
        for i in str:
            if i >= 'A' and i <= 'Z':
                ans += i.lower()
            else:
                ans += i
        return ans
